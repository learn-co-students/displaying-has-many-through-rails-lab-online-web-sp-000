<h1>Appointment</h1>

<%= link_to  @appointment.doctor.name, doctor_path(@appointment.doctor_id) %> - 
<%= link_to  @appointment.patient.name, patient_path(@appointment.patient_id) %> - <%= @appointment.appointment_datetime.strftime("%B %e, %Y - %H:%M") %>
