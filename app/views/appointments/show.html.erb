<h3>Appointment for <%= link_to @appointment.patient.name, patient_path(@appointment.patient) %><h3><br>
<p>With <%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor) %></p>
<p><%= @appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M") %></p>
