<h1>List of all the Patients:</h1>

<ul>
    <% @patients.each do |patient| %>
        <li><h2> Name: <%= link_to patient.name, patient_path(patient) %>  Number of Appointments: <%= patient.appointments.count %>  </h2></li>
    <% end %>
</ul>
