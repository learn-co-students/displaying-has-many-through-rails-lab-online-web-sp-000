<h1> <%= @patient.name %> </h1>
<h2> Appointments </h2>

<ol>
<% @patient.appointments.each do |a| %>
    <li> <%= link_to a.doctor.name, doctor_path(a.doctor_id) %> -  <%= a.appointment_datetime.strftime("%B %e, %Y at %H:%M")%></li>
<% end %>
</ol>