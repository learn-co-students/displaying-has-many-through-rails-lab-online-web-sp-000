Appointments: <%= @patient.appointments.each do |appt| %> 
    Date and Time: <%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> 
    Doctor: <%= link_to appt.doctor.name, doctor_path(appt.doctor.id) %> 
<% end %> 