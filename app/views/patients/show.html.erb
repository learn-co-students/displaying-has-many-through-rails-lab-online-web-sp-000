<h1><%= @patient.name%></h1>
<% @patient.appointments.each do |a|%>
    <ul>
        <li><%= a.appointment_datetime.strftime("%B %d, %Y at %k:%M") %></li>
        <li><%= link_to a.doctor.name, doctor_path(a.doctor) %></li><br>
        <br>
    </ul>
<% end %>