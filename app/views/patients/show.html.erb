<h1><%=@patient.name%></h1>

<ul>
  <% @patient.appointments.each do |apt| %>
    <li>Doctor: <%=link_to apt.doctor.name, doctor_path(apt.doctor)%>. Time: <%= apt.appointment_datetime.strftime("%B %e, %Y at %k:%M") %></li>
  <% end %>
</ul>
