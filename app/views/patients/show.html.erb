<h1><%= @patient.name %></h1>

<h3>Appointments</h3>

<% @patient.appointments.each do |app| %>
    <%= app.appointment_datetime.strftime("%B %d, %Y at %k:%M") %><br>
    <%= link_to app.doctor.name, doctor_path(app.doctor) %><br><br>
<% end %>