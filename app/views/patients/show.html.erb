<h1><%= @patient.name %></h1>
<h3>Appointments: </h3>
    <ul>
        <% @patient.appointments.each do |a| %>
            <li><%= link_to a.formatted_datetime + ' - ' + a.doctor.name, a.doctor %>
            <%# link_to a.formatted_datetime + ' - ' + a.doctor.name, doctor_path(a.doctor) %>
        <% end %>
    </ul>