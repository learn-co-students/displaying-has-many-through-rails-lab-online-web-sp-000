<h2><%= @patient.name %></h2>

<ul>
    <% @patient.appointments.each do |apt| %>
        <li><%= link_to apt.doctor.name, doctor_path(apt.doctor) %> <%= apt.appointment_datetime.strftime("%B %d, %Y at %k:%M") %></li>
    <% end %>
</ul>