<%= @patient.name %><br>
<% @patient.appointments.each do |appointment| %>
  <% @patient_dr = @patient.doctors.find(appointment.doctor_id) %>
  <%= link_to @patient_dr.name, doctor_path(@patient_dr) %> - <%= link_to appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M"), appointment_path(appointment) %><br>
<% end %>