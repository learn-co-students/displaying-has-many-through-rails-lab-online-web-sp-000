<% @patient.appointments.each do |appointment| %>
  <div><%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %>
  <%= appointment.appointment_datetime.strftime('%B %e, %Y at %H:%M') %></div>
<% end %>
