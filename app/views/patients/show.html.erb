<h1>Your Appointments<h1>
<ul>
<% @patient.appointments.each do |appt|%>
    <li><%=appt.appointment_datetime.strftime("%B %d, %Y at %k:%M")%> with Dr. <%=link_to appt.doctor.name, doctor_path(appt.doctor)%></li>
<%end%>
</ul>