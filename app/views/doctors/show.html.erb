<h3>Doctor's Name: <%=@doctor.name%></h3>
<h3>Department: <%=@doctor.department%></h3>
<h3>Department: <%=@doctor.department%></h3>
<h3>Appointments: </h3>

  <ol>
    <%@doctor.appointments.each do |app|%>
      <li><h5><%=app.appointment_datetime.strftime("%B %d, %Y at %H:%M")%> for <%= link_to Patient.find(app.patient_id).name, patient_path(app.patient_id)%></h5></li>
    <%end%>
  </ol>
