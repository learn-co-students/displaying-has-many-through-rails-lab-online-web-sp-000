<h1><%=@doctor.name%></h1>

<% @doctor.appointments.each do |appointment| %>
    <li>Patient: <%=link_to appointment.patient.name, patient_path(appointment.patient)%> -- <%=appointment.appointment_datetime.strftime("%B %-d, %Y at %R")%></li>
<%end%>