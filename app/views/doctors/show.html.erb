<h1><%= @doctor.name %></h1>

<ul>
<% @doctor.appointments.each do |appt| %>
    <li>Patient: <%= link_to appt.patient.name, patient_path(appt.patient) %> on <%= appt.appointment_datetime.strftime('%B %d, %Y') %> at <%= appt.appointment_datetime.strftime('%H:%M') %></li>
<% end %>
</ul>