<% @doctor.appointments.each do |a| %>
Appointment: <%= link_to a.patient.name, patient_path(a.patient) %> - <%= link_to a.appointment_datetime.strftime("%B %d, %Y at %k:%M"), appointment_path(a) %><br>
<% end %>
