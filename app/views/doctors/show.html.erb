<%= @doctor.name %><br>
<%= @doctor.appointments.each do |app|%>
    <%@new = @doctor.patients.find(app.patient_id)%>
    <%=link_to @new.name, patient_path(@new)%>
    <%= link_to app.appointment_datetime.strftime("%B %d, %Y at %k:%M"), appointment_path(app) %><br>
<%end%>
