<h1><%= @doctor.name %></h1>
<h3><%= @doctor.department %></h3>
<h3>Appointments: </h3>
    <ul>
        <% @doctor.appointments.each do |a| %>
            <li><%= link_to a.formatted_datetime + ' - ' + a.patient.name, a.patient %>
            <%# link_to a.formatted_datetime + ' - ' + a.patient.name, patient_path(a.patient) %>
        <% end %>
    </ul>