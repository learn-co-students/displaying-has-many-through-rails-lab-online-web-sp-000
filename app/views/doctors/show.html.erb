<h1>Doctor <%= @doctor.name %>'s Appointments</h1>

<ul>
  <% @doctor.appointments.each do |appt| %>
    <li>
      <%= appt.readable_date %><br>
      Patient: <%= link_to appt.patient.name, patient_path(appt.patient) %>
    </li>
  <% end %>
</ul>
