<h1><%= @doctor.name %></h1>
<% @doctor.appointments.each do |appointment| %>
    <%= appointment.time_config %> <%= link_to appointment.patient.name, patient_path(appointment.patient) %>
<% end %>