<h1><%= @doctor.name + ' - ' + @doctor.department %></h1>

<h3>Appointments</h3>
<ul>
  <% @doctor.appointments.each do |appt| %>
    <li><%= link_to appt.patient.name, patient_path(appt.patient) %> - <%= appt.appointment_datetime.strftime("%B %e, %Y at %H:%M") %></li>
  <% end %>
</ul>