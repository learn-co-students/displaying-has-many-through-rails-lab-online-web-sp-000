<ul>
  <%= @doctor.appointments.each do |appointment| %>
    <li><%= link_to appointment.patient.name,  appointment.patient %> - <%= appointment.appointment_datetime.strftime("%B %e, %Y at %k:%M")%></li>
  <% end %>
</ul>