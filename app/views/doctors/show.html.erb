<h1> Doctor <%= @doctor.name %> </h1>

<h5> Department: <%=@doctor.department%></h5>

<h3> Appointments: </h3>
<ol>
  <% @doctor.appointments.each do |a| %>
    <li>See <%= link_to a.patient.name, patient_path(a.patient) %> on <%= a.appointment_time %></li>
  <% end %>

</ol>
