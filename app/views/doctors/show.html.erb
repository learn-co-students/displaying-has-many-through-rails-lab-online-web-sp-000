<h1><%= @doctor.name %> Show Page</h1>
<h2><%= @doctor.department %> Department</h2>

<ul>
  <% @doctor.appointments.each do |appointment| %>
    <li><%= appointment.appointment_made_readable%> <%= link_to appointment.patient.name, patient_path(appointment.patient) %></li>
  <% end %>
</ul>
