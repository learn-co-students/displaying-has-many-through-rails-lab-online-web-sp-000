<h1><%= @doctor.name %></h1>

<h3><%= @doctor.department %></h3>

<% @doctor.appointments.each do |appt| %>
    <%= link_to appt.appointment_datetime.strftime("%B %e, %Y at %H:%M %P"), appointment_path(appt) %>
    <%= link_to appt.patient.name, patient_path(appt.patient) %>
<% end %>