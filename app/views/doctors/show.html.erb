<h2><%= @doctor.name %></h2>
<h3><%= @doctor.department %></h3>
<br>
<h3>Appointments: </h3>
<% @doctor.appointments.each do |appt| %>
    <p><%= link_to appt.patient.name, patient_path(appt.patient) %><br>
    at <%= appt.appointment_datetime.strftime("%B %d, %Y at %k:%M") %></p>
<% end %>