<h1>Profile for <%= @doctor.name %></h1>
<h3>Department: <%= @doctor.department %></h3>
Appointments:
<ul>
  <% @doctor.appointments.each do |appt| %>
    <li><%= link_to appt.patient.name, patient_path(appt.patient) %>:
        <%= appt.appointment_datetime.strftime("%B %-d, %Y at %k:%M") %>
  <% end %>
</ul>
