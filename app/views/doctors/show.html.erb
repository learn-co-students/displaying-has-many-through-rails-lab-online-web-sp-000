<h1><%= @doctor.name %></h1>
<h4>Department: <%= @doctor.department %></h4>
<h2>Appointments:</h2>
<ul>
  <% @doctor.appointments.each do |appointment| %>
    <li><%= link_to "#{appointment.readable_time} - #{appointment.patient.name}", patient_path(appointment.patient) %></li>
  <% end %>
</ul>
