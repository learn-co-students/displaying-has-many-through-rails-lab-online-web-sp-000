<h1>Doctor <%= @doctor.name %></h1>

<h3>Department: </h3><%= @doctor.department %>
<h3>Appointments: </h3><%= @doctor.appointments.each do |a| %>
<p>For <%= a.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></p>
<p>With <%= link_to a.patient.name, patient_path(a.patient) %></p>
<% end %>
<br>
<br>
<%= link_to 'Index', doctors_path %>
