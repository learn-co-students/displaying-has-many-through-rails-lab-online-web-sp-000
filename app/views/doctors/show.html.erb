<h1>Doctor <%= @doctor.name %></h1>
<h2>Appointments</h2>
<ol>
    <% @doctor.appointments.each do |appt| %>
        <li><%= link_to appt.patient.name, patient_path(appt.patient) %><%= appt.appointment_datetime.strftime("%B %-d, %Y at %k:%M") %></li>
    <% end %>
</ol>