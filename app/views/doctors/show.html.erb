<h1><%= @doctor.name %> appointments:</h1>

<ul>
    <% @doctor.appointments.each do |appt| %>
        <li> <%= appt.appointment_datetime.strftime("%B %d, %Y at %k:%M") %>, <%= link_to appt.patient_name, patient_path(appt.patient) %><br/>
        </li>
    <% end %>
</ul>

