<h1><%= @doctor.name %></h1>
<h3><%= @doctor.department %></h3>
<% @doctor.appointments.each do |appointment| %>
  <p><%= appointment.appointment_datetime.strftime("%B %d, %Y at %k:%M") %> - <%= link_to appointment.patient.name, appointment.patient %></p>
<% end %>