<h1> <%= @doctor.name %></h1>
<% @doctor.appointments.each do |a|%>
    <ul>
        <li><%= a.appointment_datetime.strftime("%B %d, %Y at %k:%M") %></li>
        <li><%= link_to a.patient.name, patient_path(a.patient) %></li><br>
        <br>
    </ul>
<% end %>