<h1>Doctor Name: <%= @doctor.name %></h1>
<h3>Appointments</h3>
<ul>
  <% @doctor.appointments.each do |a| %>
    <li><%= link_to a.patient.name, patient_path(a.patient) %><br><%= a.readable_time %></li>
  <% end %>
</ul>