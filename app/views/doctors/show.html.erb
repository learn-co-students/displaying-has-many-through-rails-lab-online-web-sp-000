Doctor: <%=@doctor.name%>
Department: <%=@doctor.department%>
Appointments:
<%@doctor.appointments.each do |appointment|%>
    <%=link_to appointment.patient.name, patient_path(appointment.patient)%>
    <br><%=appointment.appointment_datetime.strftime("%B %d, %Y at %R")%>
<%end%>