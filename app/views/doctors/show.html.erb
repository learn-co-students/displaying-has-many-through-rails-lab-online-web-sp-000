<h2><%= @doctor.name %></h2>
<h2><%= @doctor.department %></h2>

<% @doctor.appointments.each do |appt| %>
    <%= appt.date_time %><%= link_to appt.patient.name, patient_path(appt.patient) %>
  <% end %>